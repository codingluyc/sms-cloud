spring:
  application:
    name: sms-cache
  profiles:
    active: dev
#  nacos配置
  cloud:
    nacos:
      discovery:
        server-addr: www.sms.com:8848
      config:
        server-addr: www.sms.com:8848
        file-extension: yml
        # nacos的配置文件data id应该是 sms-api-dev.yml