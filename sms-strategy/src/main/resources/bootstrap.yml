# 服务名称
spring:
  application:
    name: sms-strategy
# 多环境
  profiles:
    active: dev
# nacos配置
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.3.9:8848
      config:
        server-addr: 192.168.3.9:8848
        file-extension: yml
        # nacos的配置文件data id应该是 sms-api-dev.yml
